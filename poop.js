function procCmd(cmd) {
    cmd = cmd.toLowerCase();
    if (cmd == "iamcool"){
        clientMessage("How could you?");
        getPlayer->setHealth(0);
        
    }
    if (cmd == "adamiscool"){
        clientMessage("Thank you! :D");
        getPlayer->addItemInventory(264, 64)
}
}
